// initial state
const state = {
  menu: []
}

// getters
const getters = {
}

// mutations
const mutations = {
  setMenu (state, payload) {
    state.menu = payload
  }
}

// actions
const actions = {
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
